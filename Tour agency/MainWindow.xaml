<Window x:Class="Tour_agency.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tour_agency"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="1000" >
    <Grid>
        <Button Width="90" VerticalAlignment="Top" Margin="50" Click="Button_Click"/>

        <Frame x:Name="Main" Margin="0 100 0 0" />
        <TabControl x:Name="yyy" Margin="0 100 0 0">
            <TabItem Header="awdad" x:Name="ttt" IsSelected="True" />
            <TabItem Header="qwerty" x:Name="www" IsSelected="False" />
        </TabControl>

    </Grid>
</Window>
